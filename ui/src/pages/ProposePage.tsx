/**
 * Propose page — form for council members to create a new upgrade proposal.
 *
 * Fields:
 * - Target contract ID
 * - New WASM hash (or upload .wasm for auto-hash)
 * - Description / change summary
 *
 * TODO: implement once UpgradeManager.proposeUpgrade() is available.
 */
export function ProposePage() {
  return (
    <div>
      <h2>New upgrade proposal</h2>
      {/* TODO: ProposalForm component */}
    </div>
  );
}
