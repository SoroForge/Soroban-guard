/**
 * Root application component.
 *
 * Pages to be implemented:
 * - /           → Dashboard (open proposals, vote tallies, timelock queue)
 * - /proposals  → Full proposal list with filtering
 * - /propose    → Create a new upgrade proposal
 * - /history    → On-chain upgrade audit log
 */
export function App() {
  return (
    <div>
      <h1>Soroban Upgrade Guard</h1>
      <p>Governance dashboard — coming soon.</p>
      {/* TODO: add routing and page components */}
    </div>
  );
}
