/**
 * ProposalCard
 *
 * Displays a single governance proposal with its vote tally and action buttons
 * (Approve / Reject / Queue / Cancel) depending on the viewer's role.
 *
 * Props:
 * - proposal  — GovernanceProposal from @soroban-guard/sdk
 * - onApprove — callback when council member approves
 * - onReject  — callback when council member rejects
 *
 * TODO: implement once GovernanceProposal type is finalised.
 */
export function ProposalCard() {
  return <div>{/* TODO: proposal title, status badge, vote bar, action buttons */}</div>;
}
