/**
 * WalletConnect
 *
 * Button / pill that shows wallet connection state and handles
 * connect / disconnect actions via the useWallet hook.
 *
 * States:
 * - Not installed  → "Install Freighter" link
 * - Not connected  → "Connect Wallet" button
 * - Connected      → truncated public key + disconnect option
 *
 * TODO: implement using useWallet hook.
 */
export function WalletConnect() {
  return <div>{/* TODO: wallet connection button */}</div>;
}
