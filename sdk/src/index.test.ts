import { describe, it, expect } from "vitest";

describe("SDK", () => {
  it("should export types and utilities", () => {
    // Basic smoke test to ensure SDK exports are valid
    expect(true).toBe(true);
  });
});
